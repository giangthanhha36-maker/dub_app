# Gradio stack — cai TRUOC omnivoice (tranh pip backtracking typer/httpx rat lau)
#
# omnivoice keo transformers 5.x -> can huggingface_hub>=1.5 (co is_offline_mode, ...)
# Gradio 4.44.x can huggingface_hub<1.0 (HfFolder) -> KHONG tuong thich transformers 5.x
# Gradio 5.7.1+ da bo HfFolder -> dung chung hf_hub 1.x voi transformers
gradio>=5.7.1,<6
huggingface_hub>=1.5.0,<2.0
# Ghim typer tranh pip thu hang tram phien ban
typer>=0.12,<0.13
