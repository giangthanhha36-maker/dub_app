# ===========================================================================
# Moi truong RIENG cho OmniVoice API (audio.py). Python 3.12 + CUDA.
#
# QUAN TRONG: cai theo thu tu trong setup_omnivoice.sh
#   1) torch GPU
#   2) requirements-omnivoice-base.txt  (gradio stack — cai TRUOC)
#   3) requirements-omnivoice.txt       (omnivoice + audio libs)
# ===========================================================================

# Gradio stack phai cai truoc omnivoice (tranh pip backtracking typer rat lau)
# Xem requirements-omnivoice-base.txt

omnivoice
numpy
pysrt
librosa
soundfile
PyYAML>=6.0
