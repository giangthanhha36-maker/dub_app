# ===========================================================================
# Thu vien dub_app — dung MOI TRUONG conda "omnivoice" (xem setup_omnivoice.sh).
#
# Cai dat:
#   conda activate omnivoice
#   pip install -r requirements-omnivoice-base.txt
#   pip install -r requirements-omnivoice.txt
#
# Hoac chay ./run.sh — tu dong cai neu thieu.
# ===========================================================================
-r requirements-omnivoice-base.txt
-r requirements-omnivoice.txt
